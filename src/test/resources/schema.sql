-- Schema initialization for tests
USE CIS;

-- A tabela será criada automaticamente pelo Hibernate devido ao ddl-auto=create-drop
-- Este arquivo é apenas para referência e inicializações adicionais se necessário